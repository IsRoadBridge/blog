<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zxq.dao.BlogMapper">

    <select id="findBlogAll" resultType="Blog">
        select  * from t_blog
    </select>

    <select id="findById" resultType="Blog" parameterType="java.lang.Long">
        select  * from t_blog  where  id=#{id}
    </select>

    <insert id="saveBlog" parameterType="Blog">
        insert  into t_blog(id,title,content,first_picture,flag,views,appreciation,copyright,
                        commentabled,published,recommend,create_time,update_time,type_id,user_id,tag_id)
              values(#{id},#{title},#{content},#{firstPicture},#{flag},#{views},#{appreciation},
              #{copyright},#{commentabled},#{published},#{recommend},#{createTime},#{updateTime},
              #{type.id},#{user.id},#{tagIds})
    </insert>

    <update id="updateBlog" parameterType="Blog">
       update  t_blog set title=#{title},content=#{content},first_picture=#{firstPicture},
       flag=#{flag},views=#{views},appreciation=#{appreciation},copyright=#{copyright},
       commentabled=#{commentabled},published=#{published},recommend=#{recommend},create_time=#{createTime},
       update_time=#{updateTime},type_id=#{type.id},user_id=#{user.id},tag_id=#{tagIds} where id=#{id}
    </update>

    <delete id="deleteBlog" parameterType="java.lang.Long">
        delete  from  t_blog where  id=#{id}
    </delete>
</mapper>